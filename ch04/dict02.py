d3 = {'one':'일', 'two':'이', 'three':'삼', 'four': '사'}
print("d3.keys()->", d3.keys())       # 키만 리스트로 추출함, 임의의 순서
print("d3.values()->", d3.values())   # 값만 리스트로 추출함, 임의의 순서
print("d3.items()->", d3.items())     # 키와 값의 튜플을 리스트로 반환함
